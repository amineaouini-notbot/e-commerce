<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <div class="container">
        <h1><%= product.title %></h1>

        <h3><%= product.price %> - USD</h3>
        <form action="/products/addtoCart" method="post">
            <input type="number" name="product_id" value="<%=product.id%>" hidden>
            <input type="number" name="cart_id" value="<%=cart.id%>" hidden>
            <button type="submit">Add to Cart</button>
        </form>
        <div style="display: flex; margin-bottom: 50px;">
            <% product.images.forEach(image => {%>
                <div style="margin-right: 20px;">
                    <img src="/upload/<%= product.id %>/<%= image %>" alt="" style="height: 100px;">
                </div>
                
            <%})%>
        </div>
        
        <div>
            <img src="/upload/<%= product.id %>/<%= product.images[0] %>" alt="" height="400px">
        </div>

        
    </div>
</body>
</html>